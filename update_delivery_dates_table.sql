-- ============================================
-- ОБНОВЛЕНИЕ: Добавление поля для исключений
-- ============================================
-- 
-- Добавляем поле для хранения исключений (дни, когда доставка НЕ осуществляется)
-- Например: "кроме 16" или "кроме 16, 20, 25"
-- 
-- ИНСТРУКЦИЯ:
-- 1. Откройте SQL Editor в Supabase
-- 2. Скопируйте и выполните этот запрос
-- 
-- ============================================

-- Добавляем поле для исключений (дни без доставки)
ALTER TABLE delivery_dates 
ADD COLUMN IF NOT EXISTS restrictions TEXT DEFAULT NULL;

-- Комментарий к полю
COMMENT ON COLUMN delivery_dates.restrictions IS 'Дни, когда доставка НЕ осуществляется. Формат: "16" или "16, 20, 25" (номера дней месяца)';

-- Пример обновления данных (можно использовать для тестирования):
-- UPDATE delivery_dates SET restrictions = '16' WHERE city_name = 'Москва';
